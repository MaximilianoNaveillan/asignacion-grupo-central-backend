{
  "name": "server",
  "version": "1.0.0",
  "description": "backend ( visor para la información sobre de las causas de muerte en EEUU )",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server",
    "dev": "nodemon server --ignore collection.json"
  },
  "author": "Maximiliano Naveillan",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "node-fetch": "2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
